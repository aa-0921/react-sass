import { ZeitUIThemesPalette } from 'components/styles/themes';
export declare type SnippetStyles = {
    color: string;
    border: string;
    bgColor: string;
};
export declare const getStyles: (type: "default" | "secondary" | "success" | "warning" | "error" | "dark" | "lite", palette: ZeitUIThemesPalette, fill?: boolean | undefined) => SnippetStyles;
